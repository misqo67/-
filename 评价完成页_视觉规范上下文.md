# 评价完成页_视觉规范上下文

> 本文档由 AI 根据设计稿截图 + SVG 底图 token + 两层设计规范自动整理，作为生码的唯一依据。
> 设计基准：780px 宽度（@2x），所有数值均为 @2x 像素值。
> 基底来源：`内容产品/review-complete.html`（底图包 v1.0.1）

---

## 一、编码强制约束（来自 coding-constraints.md）

| # | 约束 | 执行细则 |
|---|------|----------|
| 1 | 严禁 emoji 当图标 | 必须用 Leez Icon 组件（Icon Font 方案），Icon 库缺失时用灰色占位块（`#CCCCCC`，圆角 4px），会话结尾统一列出占位位置 |
| 2 | 价格必须用 Price 组件 | 本页无金额展示，不涉及 |
| 3 | 手机端屏幕宽度自适应 | 使用 vw 方案，设计稿 px ÷ 780 × 100 = vw |
| 4 | 严禁想象/篡改规范 | 所有参数均来自设计稿截图 + 底图 SVG token，不自行补值 |

---

## 二、本页面视觉调色板

### 可用颜色

| 用途 | 色值 | 来源 |
|------|------|------|
| 页面背景 | `#F8F9FB` | 底图 SVG rect fill |
| 顶部渐变底 1 | `#FFFCF4` → `#FFFEFC` → 透明 | 底图 SVG linearGradient |
| 顶部渐变底 2 | `#FFE5D9` → 透明 | 底图 SVG linearGradient（暖粉渐变） |
| 卡片白底 | `#FFFFFF` | 底图 SVG + 设计稿 |
| 成功勾选绿 | `#30D158` | 底图 SVG ellipse fill / 色彩规范「成功绿」 |
| 勾选内部色 | `#FFFBEC` | 底图 SVG path fill |
| 品牌橙（主 CTA） | `#FF6633` | 底图 SVG + 色彩规范「品牌主色」 |
| 强调数字橙 | `#FF661A` | 底图 SVG text fill（数字「23」和帮助人数） |
| 一级文字色 | `#121A2F` | 底图 SVG text fill（蓝灰色系） |
| 二级文字色 | `#7F8A9E` | 底图 SVG text fill（蓝灰色系） |
| 白色文字 | `#FFFFFF` | CTA 按钮文字 |
| 按钮线性橙边框 | `#FF6633` | 设计稿「查看评价详情」按钮描边 |

### 可用字号（@2x）

| 字号 | 字重 | 本页面用途 |
|------|------|-----------|
| 34px | Semibold | 顶部导航「完成」按钮 |
| 32px | Medium(500) | 「发布第 X 条评价」主信息文字 |
| 24px(特殊) | Bold | 评价数字「23」（品牌数字体/强调） |
| 30px | Bold | 「查看评价详情」按钮文字 |
| 28px | Regular(400) | 「累计帮助 X 万用户」辅助文字、奖励卡片标题 |
| 26px | Medium(500) | 「写评价」按钮文字 |
| 24px | Regular(400) | 奖励卡片有效期、推荐列表副信息 |
| 28px | Medium(500) | 推荐列表商户名称 |
| 24px | Regular(400) | 推荐列表区域/品类/打卡信息 |
| 32px | Medium(500) | 模块标题「期待你写更多真实有帮助的评价」 |

### 可用间距（@2x）

| 值 | 本页面用途 |
|----|-----------|
| 20px | 页面左右边距（卡片外边距） |
| 24px | 卡片内边距、推荐列表项图文间距 |
| 16px | 推荐列表项间分割间距 |
| 20px | 模块间距（卡片分割） |
| 12px | 奖励卡片内部元素间距 |

### 可用圆角（@2x）

| 值 | 本页面用途 |
|----|-----------|
| 24px | 奖励卡片区域外容器、推荐列表外容器 |
| 12px | 推荐列表内商户图片、横幅轮播 |
| 40px | 「查看评价详情」按钮（高度80px，胶囊圆角） |
| 25px(≈26px) | 「写评价」小按钮（高度约50px，胶囊圆角=h/2） |
| 6px | 推荐列表商户图片 |

---

## 三、模块排版策略（L1 · 模块布局）

### 模块 A：顶部成功信息区

**布局模式**：居中纵向堆叠，暖色渐变背景

```
┌────────────────────────────────────┐
│     [分享评价]          [完成]      │  ← 顶部导航栏
│                                    │
│            ✓ 成功勾选               │  ← 绿色圆形勾选图标
│       发布第 23 条评价              │  ← 主信息行
│       累计帮助 18883 万用户         │  ← 辅助信息行
│                                    │
│     ┌─────────────────────┐        │
│     │   查看评价详情        │        │  ← 线性按钮（二级）
│     └─────────────────────┘        │
│                                    │
│    [ ═══ 横幅轮播 ═══ ]             │  ← banner 轮播区
└────────────────────────────────────┘
```

### 模块 B：双奖励卡片区

**布局模式**：双卡片等宽并排（运营位卡片变体）

```
┌──────────────────────────────────┐
│  ┌──────────┐  ┌──────────┐      │
│  │  [奖励图] │  │ [分享图]  │      │
│  │恭喜获得5元│  │我的评价帮 │      │
│  │  打车券   │  │助了18883人│      │
│  │2028.7.30前│  │分享这份认可│      │
│  │  有效     │  │           │      │
│  │ [去领取]  │  │ [去分享]   │      │
│  └──────────┘  └──────────┘      │
└──────────────────────────────────┘
```

- 两卡等宽，间距 12px
- 左卡「去领取」：面性按钮（一级），品牌橙底白字
- 右卡「去分享」：线性按钮（二级），品牌橙边框+橙字

### 模块 C：推荐评价列表

**布局模式**：标题 + 左图右文列表

```
┌──────────────────────────────────┐
│  期待你写更多真实有帮助的评价       │  ← 模块标题
│  ┌─────────────────────────────┐ │
│  │ [图] 心形灯笼               │ │
│  │ 64×64 城隍庙/豫园 出片点     │ │
│  │ r6   近一个月500人打卡过 [写评│ │
│  │      价]                     │ │
│  ├─────────────────────────────┤ │
│  │ [图] 盘发小生               │ │
│  │ ... 同上结构                 │ │
│  ├─────────────────────────────┤ │
│  │ ... 更多列表项               │ │
│  └─────────────────────────────┘ │
│            查看全部 >             │
└──────────────────────────────────┘
```

**每个列表项精确参数：**

| 层级 | 内容 | 字号 | 字重 | 颜色 | 规则 |
|------|------|------|------|------|------|
| 图片 | 商户图 | — | — | — | 128×128px(@2x)=64×64，圆角 12px(@2x)=6px |
| 商户名 | 店铺名称 | 28px | Medium(500) | `#121A2F` | 最多1行截断 |
| 区域品类 | 「南翔  酸奶鲜奶」 | 24px | Regular(400) | `#7F8A9E` | 空格或竖线分隔 |
| 打卡/消费信息 | 「2025-07-06 打卡」 | 24px | Regular(400) | `#7F8A9E` | 第三行 |
| 写评价按钮 | 「写评价」 | 26px | Regular(400) | `#FFFFFF` | 面性胶囊，背景 `#FF6633`，高度约50px，圆角25px |

---

## 四、组件视觉参数（L2 · 组件规范，按需补充）

### 按钮

| 层级 | 背景 | 文字色 | 高度(2x) | 圆角 | 字号/字重 | 用在哪里 |
|------|------|--------|----------|------|-----------|---------|
| 二级（线性/橙） | 透明 + `#FF6633` 描边 | `#FF6633` | 80px | 40px（胶囊=h/2） | 30px Medium | 查看评价详情 |
| 一级（面性/橙） | `#FF6633` | `#FFFFFF` | 50px | 25px（胶囊=h/2） | 26px Regular | 写评价、去领取 |
| 二级（线性/橙小） | 透明 + `#FF6633` 描边 | `#FF6633` | 50px | 25px | 26px Regular | 去分享 |

### 成功图标

| 属性 | 值 |
|------|-----|
| 尺寸 | 68px(@2x)=34px 圆形 |
| 背景色 | `#30D158` |
| 勾选色 | `#FFFBEC` |

---

## 五、关键设计决策摘要

| # | 注意事项 | 具体要求 |
|---|---------|---------|
| 1 | 页面使用蓝灰色系 | 底图一级字色 `#121A2F`，二级 `#7F8A9E`，背景 `#F8F9FB` |
| 2 | 顶部区域暖色渐变 | 两层渐变叠加：`#FFFCF4→透明` + `#FFE5D9→透明`，模拟柔和暖粉氛围 |
| 3 | 数字强调使用品牌橙 | 评价数「23」和帮助人数「18883」使用 `#FF661A` 加粗 |
| 4 | 双奖励卡片并排 | 等宽排列，左卡面性CTA，右卡线性CTA，按设计稿 |
| 5 | 推荐列表使用白色容器 | 外容器白底圆角24px(@2x)=12px，列表项间分割线分隔 |
| 6 | 图标占位 | 顶部导航「分享评价」图标、成功勾选 ✓ 使用灰色占位或 SVG 还原 |
| 7 | vw 自适应 | 全部尺寸按 vw 换算，390px 逻辑基准 |
