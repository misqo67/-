# 分享评价半浮层_视觉规范上下文

> 本文档根据 IMD《【分享专项】设计过程稿》“唤起”页面、用户提供的截图和命中设计规范整理，作为本次 HTML Demo 的实现依据。
> 设计基准为 780px 宽度（@2x）。所有代码尺寸均使用 `vw` 映射，换算规则为：设计稿 px ÷ 780 × 100 = vw。
> 基底来源：`/Users/sheyuchen/Desktop/分享率/评价完成页.html`。

## 一、编码强制约束

| 约束 | 执行细则 |
|---|---|
| 图标 | 图标只允许使用 Leez Icon。当前 HTML Demo 未能确认该分享渠道的 Leez class，因此用有品牌色彩的圆形资源占位；关闭符号以文字“×”呈现，并提供无障碍标签。业务接入时应替换为对应的 Leez Icon 或业务渠道资源。 |
| 自适应 | 页面根容器宽度为 100%，所有布局尺寸、字号、间距均使用 `vw`。 |
| 组件路由 | 分享抽屉命中 `ShareViewSheet` / `BottomSheetsShareSheet`，采用 `Key + Regular` 分享动作区域，并按 IMD 增加“生成分享图”三列宫格区域。 |
| 蒙层 | 用户明确要求 `#111111`、80% 透明度，此需求优先于通用 Sheet 的默认蒙层 token。 |

## 二、本页面视觉调色板

| 用途 | 色值 | 来源 |
|---|---|---|
| 蒙层 | `rgba(17,17,17,.80)` | 用户明确需求 |
| 半浮层背景 | `#FFFFFF` | IMD 截图、浮层规范 |
| 主文字 | `#111111` | 浮层/文字规范 |
| 辅助文字 | `#666666` | 文字规范 |
| 弱辅助文字 | `#888888` | 文字规范 |
| 图片占位底 | `#F3F3F3` | IMD 截图中的分享图骨架屏 |
| 非 UGC 图片描边 | `rgba(17,17,17,.04)` | 图片容器规范 |

| 设计 token | 用途 |
|---|---|
| 36px / 500 / 52px 行高 | “分享到”标题 |
| 28px / 400 / 40px 行高 | 分享渠道名称、生成分享图标题 |
| 24px / 400 / 32px 行高 | 分享图名称 |
| 28px 顶部圆角 | 设计稿风格 Sheet 顶部圆角 |
| 24px 左右栅格 | 图片宫格及内容区左右内边距 |
| 12px 图片圆角 | 大于 100px 的分享图卡片 |

## 三、模块排版 / 状态路由策略

### 模块 A：模态遮罩

页面点击“分享评价”或“去分享”后进入 `opening → open` 状态，遮罩覆盖整个视口，背景为 `#111111` 且不透明度为 80%。遮罩存在时锁定底页滚动，点击遮罩或关闭按钮进入 `closing → closed`。

### 模块 B：分享到分享渠道

半浮层从底部向上弹出，白色背景，仅顶部左右圆角。标题“分享到”左对齐，右侧为关闭入口。渠道区域横向滚动，按“渠道图标 + 渠道名”纵向排列，保持六个入口在当前视图内可见，并在窄屏时允许滑动。

| 元素 | 规格 |
|---|---|
| 浮层位置 | fixed，底部对齐，z-index 高于蒙层 |
| 浮层高度 | 内容驱动，最大高度 80vh，内容区可滚动 |
| 顶部圆角 | 28px（@2x）→ 3.59vw |
| 标题 | 36px / 500 / `#111111`（@2x）→ 4.62vw |
| 关闭入口 | 36px / `#888888`（@2x）→ 4.62vw |
| 渠道图标 | 圆形 64px（@2x）→ 8.21vw |
| 渠道名称 | 20px / 400 / `#111111`（@2x）→ 2.56vw |

### 模块 C：生成分享图

标题下以 3 列宫格展示 6 张分享图模板。图片使用 3:4 比例、12px 圆角、缩放裁切；当前缺少真实模板切图时展示骨架占位，不模拟具体业务图案。点击图片会切换选中态并在底部显示轻量提示。

| 元素 | 规格 |
|---|---|
| 区域标题 | 28px / 400 / `#111111`（@2x）→ 3.59vw |
| 宫格列数 | 3 列 |
| 图间距 | 16px（@2x）→ 2.05vw |
| 宫格图片 | 3:4、圆角 12px（@2x）→ 1.54vw |
| 图片描边 | 1px `rgba(17,17,17,.04)` |
| 模板名称 | 20px / 400 / `#666666`（@2x）→ 2.56vw |

## 四、代码组件参数映射

React / Picasso 业务实现中，分享动作使用 `ShareViewSheet`（或版本兼容的 `BottomSheetsShareSheet`）：`visible` 由页面状态控制；`title="分享到"`；`keyActions` 承载微信好友、朋友圈、私信好友等核心渠道；`regularActions` 承载复制链接、QQ 等辅助渠道；`onClose` 关闭当前半浮层；`onActionSelect` 处理渠道触达。模板图片宫格为该组件之外的业务扩展内容，置于 Sheet 内容区。

## 五、关键设计决策摘要

| 决策 | 实现 |
|---|---|
| Sheet 类型 | 模态底部分享抽屉，而不是居中对话框或顶部浮层菜单。 |
| 关闭路径 | 右上角关闭、点击蒙层、Esc 三种方式均可关闭。 |
| 蒙层 | 严格使用用户指定的 `#111111` / 80%。 |
| 视觉还原 | 白底、顶部圆角、渠道横排与 3 列分享图宫格与 IMD “唤起”设计一致。 |
| 资源缺口 | 六个渠道图标及六张分享图模板没有交付切图；Demo 用骨架占位，业务接入时替换为正式资源。 |
